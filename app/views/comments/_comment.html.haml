- unless comment.body.nil?
	.jumbotron
		Comment added by:
		%br
		%b
			= comment.user.try(:email)
		%p
		Content:
		%br
		= comment.body
		%p
		Date of adding:
		%br
		= comment.created_at
		%p
		- if user_signed_in? && comment.user_id == current_user.id
			= link_to 'Delete comment', [comment.movie, comment], method: :delete, data: {confirm: 'Are you sure you want to delete this comment?'}